#include <string>

#ifndef preprocess_asm_stream_HPP
#define preprocess_asm_stream_HPP

std::string preprocess_asm_stream(std::string asm_stream);

#endif


#ifndef assemble_HPP
#define assemble_HPP

std::string assemble(std::string asm_stream);

#endif